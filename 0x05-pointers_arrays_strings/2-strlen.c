#include <stdio.h>
#include "main.h"

/**
 * _strlen - prints len of str
 * @s: argument
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	for (int i = 0; s[i] != '\0'; i++)
	{
	
	}
	return (i);
}
