#include <stdio.h>
#include "main.h"

/**
 * _strlen - prints len of str
 * @s: argument
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	for (int b = 0; s[b] != '\0'; b++)
	{
	
	}
	return (b);
}
